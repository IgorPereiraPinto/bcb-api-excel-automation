# ═══════════════════════════════════════════════════════════════
# Variáveis de ambiente — BCB API Excel Automation
# ═══════════════════════════════════════════════════════════════
#
# COMO USAR:
#   1. Copie este arquivo: cp .env.example .env
#   2. Ajuste os valores conforme seu ambiente
#   3. O .env é ignorado pelo Git (não vai para o repositório)
#
# NOTA: Todas as variáveis são OPCIONAIS. Se não definidas,
#       o script usa os valores padrão indicados abaixo.
# ═══════════════════════════════════════════════════════════════

# ── Caminhos ──────────────────────────────────────────────────
# Arquivo de configuração das séries
SERIES_CONFIG_PATH=config/series_config.yaml

# Arquivo Excel de saída (será criado/atualizado automaticamente)
OUTPUT_EXCEL_PATH=data/output/indicadores_bcb.xlsx

# Arquivo de log
LOG_PATH=data/output/run.log

# ── API ───────────────────────────────────────────────────────
# Timeout em segundos por requisição à API SGS
API_TIMEOUT=30

# Número de tentativas em caso de falha
API_RETRIES=2

# ── Período padrão ────────────────────────────────────────────
# Dias retroativos quando nenhum período é informado via CLI
# Use 3 para execução diária (cobre fins de semana)
# Use 730 para carga inicial (2 anos de histórico)
DAYS_BACK_DEFAULT=3
